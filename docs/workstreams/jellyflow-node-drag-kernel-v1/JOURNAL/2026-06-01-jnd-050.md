# JND-050 Session Journal - Gesture Trace And XyFlow Projection

Date: 2026-06-01
Status: DONE

## Scope

Recorded node drag start/update/end behavior through the renderer-neutral gesture stream,
interaction harness, and XyFlow-compatible callbacks.

## Changes

- Added `NodeDragStart`, `NodeDragUpdate`, `NodeDragEnd`, and `NodeDragEndOutcome` to
  `runtime::events`.
- Split runtime gesture event ownership into focused `connection`, `node_drag`, and `gesture`
  event modules.
- Added `NodeGraphGestureEvent` variants for node drag start/update/end.
- Wired node drag gesture events through XyFlow-compatible callback dispatch.
- Changed `NodeGraphGestureCallbacks::on_node_drag` to receive a full `NodeDragUpdate` payload.
- Extended the interaction harness callback trace with node drag start/update/end callback events.
- Added adapter conformance coverage for pointer payloads, drag transaction commits, node-change
  projection, event ordering, and callback payloads.
- Added public surface smoke coverage for the new runtime event and XyFlow re-export.

## RED Evidence

- `cargo nextest run -p jellyflow-runtime adapter_conformance_harness_records_node_drag_gesture_transaction_and_callbacks`
  - Failed before node drag gesture events, callback trace variants, and callback dispatch existed.

## GREEN Evidence

- `cargo nextest run -p jellyflow-runtime adapter_conformance_harness_records_node_drag_gesture_transaction_and_callbacks`:
  passed.
- `cargo nextest run -p jellyflow-runtime adapter_conformance`: passed, 8 tests.
- `cargo nextest run -p jellyflow-runtime --test public_surface`: passed, 2 tests.
- `cargo check -p jellyflow-runtime`: passed.
- `cargo fmt --check`: passed.
- `jq empty docs/workstreams/jellyflow-node-drag-kernel-v1/WORKSTREAM.json`: passed.
- `git diff --check`: passed.
- `cargo nextest run -p jellyflow-runtime`: passed, 150 tests.
- `cargo clippy -p jellyflow-runtime --all-targets -- -D warnings`: passed.

## Notes

Pointer capture, DOM no-drag selectors, drag handles, and renderer smoke tests remain adapter-owned.
The runtime accepts canvas-space pointer payloads that adapters have already normalized.
