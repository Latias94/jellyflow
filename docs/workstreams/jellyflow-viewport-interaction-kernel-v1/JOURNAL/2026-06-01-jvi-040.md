# JVI-040 Journal - Viewport Conformance Fixtures

Date: 2026-06-01

## Scope

- Added viewport pan/zoom intent actions to the conformance fixture vocabulary.
- Recorded viewport view callbacks and viewport move callbacks in conformance traces.
- Added a fixture scenario that proves viewport pan and zoom state changes, gesture payloads, and
  callback ordering.
- Converted the adapter viewport/selection ordering trace to the fixture runner.

## Implementation Notes

- `ConformanceAction::apply_viewport_pan` and `apply_viewport_zoom` delegate to
  `NodeGraphStore::apply_viewport_pan` and `apply_viewport_zoom`.
- `ConformanceCallbackEvent` now includes view callback events and viewport move callback events.
- The viewport fixture records store `ViewChanged` before callback events, matching current
  subscription ordering.

## Validation

- `cargo nextest run -p jellyflow-runtime conformance` passed, 12 tests run.
- `cargo nextest run -p jellyflow-runtime adapter_conformance` passed, 8 tests run.
- `cargo check -p jellyflow-runtime` passed.

## Next

JVI-050 should update README/runtime README guidance, run the broader closeout gate, and decide
whether remaining renderer smoke, adapter runner helpers, animation, or auto-pan items should split
into follow-ons.
