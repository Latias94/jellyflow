# JVI-030 Journal - Store Viewport Intent And Callbacks

Date: 2026-06-01

## Scope

- Applied viewport pan/zoom intent through `NodeGraphStore` view-state publication.
- Added runtime viewport move gesture payloads and gesture event variants.
- Wired XyFlow-style viewport move callbacks through the existing callback installer.
- Kept conformance fixture authoring for JVI-040.

## Implementation Notes

- `NodeGraphStore::apply_viewport_pan` and `apply_viewport_zoom` reuse `runtime::viewport` math and
  delegate publication to `set_viewport`.
- `ViewportMoveStart`, `ViewportMove`, and `ViewportMoveEnd` live in `runtime::events`; xyflow
  callback types re-export the same payloads.
- `NodeGraphGestureCallbacks` now includes `on_move` in addition to `on_move_start` and
  `on_move_end`.
- Focused viewport tests assert view-change emission and gesture/callback ordering without any
  renderer dependency.

## Validation

- `cargo nextest run -p jellyflow-runtime viewport` passed, 14 tests run.
- `cargo nextest run -p jellyflow-runtime explicit_modules_expose_their_owned_surfaces` passed, 1
  test run.
- `cargo nextest run -p jellyflow-runtime adapter_conformance` passed, 8 tests run.
- `cargo check -p jellyflow-runtime` passed.
- `cargo fmt --check` passed.
- `jq empty docs/workstreams/jellyflow-viewport-interaction-kernel-v1/WORKSTREAM.json
  docs/workstreams/jellyflow-viewport-interaction-kernel-v1/TASKS.jsonl` passed.
- `git diff --check` passed.

## Next

JVI-040 should add viewport conformance fixture scenarios and decide whether the existing adapter
viewport ordering trace should move to the fixture runner.
