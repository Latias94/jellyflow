# JPIS-030 Journal - Conformance And Template Inertia Replay

Date: 2026-06-02
Status: DONE

## Summary

Added conformance actions and template smoke coverage for sampled viewport pan inertia frames.

## TDD Notes

- Red: `cargo nextest run -p jellyflow-runtime adapter_conformance_fixture_runner_applies_viewport_pan_inertia_frames` failed because the pan inertia conformance action constructors did not exist.
- Green: added pan inertia frame apply/assert/reject actions and runner support.

## Decisions

- Conformance replay uses the pure runtime planner and publishes frames through `store.set_viewport`.
- The runner keeps adapter velocity estimation, frame clocks, cancellation, and renderer smoke out of
  `jellyflow-runtime`.
- Template expected traces derive frame values from the public planner instead of duplicating decay
  math.

## Evidence

- `cargo fmt --check`: passed.
- `cargo nextest run -p jellyflow-runtime conformance`: passed, 51 tests run.
- `cargo nextest run -p jellyflow-runtime adapter_conformance`: passed, 16 tests run.
- `cargo test --manifest-path templates/headless-adapter/Cargo.toml`: passed, 7 tests run.
- `cargo run --manifest-path templates/headless-adapter/Cargo.toml -- check`: passed, 4 scenarios.

## Next

Implement JPIS-040 documentation and closeout gates.
