# JAI-020 Session Journal

Date: 2026-06-01

## Summary

Added the first headless auto-pan runtime slice.

- Added public `runtime::auto_pan`.
- Added `AutoPanActivation`, `AutoPanRequest`, `AutoPanPlan`, and `AutoPanOutcome`.
- Added pure `compute_auto_pan` frame math using logical screen coordinates, viewport size,
  elapsed seconds, and `NodeGraphAutoPanTuning`.
- Added `NodeGraphStore::apply_auto_pan` through existing viewport pan publication.
- Added focused tests for sign convention, activation policy, invalid/no-op input, and store
  publication.
- Added public-surface smoke coverage.

## Validation

- RED: `cargo nextest run -p jellyflow-runtime auto_pan` failed before `runtime::auto_pan` existed.
- GREEN: `cargo nextest run -p jellyflow-runtime auto_pan` passed, 4 tests.
- `cargo nextest run -p jellyflow-runtime --test public_surface` passed, 3 tests.
- `cargo check -p jellyflow-runtime` passed.

## Next

Use JAI-030 to make auto-pan replayable from conformance fixtures and assert viewport/callback
ordering.
