# JIH-030 Selection Box Fixture

Date: 2026-06-01

## Completed

- Added `runtime::selection` for renderer-neutral marquee selection.
- Added `SelectionBoxOptions`, `SelectionBoxResult`, `compute_selection_box`, and
  `NodeGraphStore::apply_selection_box`.
- Added harness-backed fixtures for replacement and additive selection.
- Covered hidden-node exclusion, node/edge selectable policy, connected-edge selection, sorted
  output, and emitted selection events.

## Validation

- RED: `cargo nextest run -p jellyflow-runtime selection` failed before `runtime::selection` and
  `InteractionHarness::store_mut` existed.
- `cargo fmt --check`: passed after formatting.
- `cargo nextest run -p jellyflow-runtime selection`: passed, 6 tests.
- `cargo nextest run -p jellyflow-runtime --test public_surface`: passed, 2 tests.
- `cargo check -p jellyflow-runtime`: passed.
- `cargo nextest run -p jellyflow-runtime`: passed, 141 tests.
- `cargo clippy -p jellyflow-runtime --all-targets -- -D warnings`: passed.
- `jq empty docs/workstreams/jellyflow-interaction-harness-v1/WORKSTREAM.json`: passed.
- `git diff --check`: passed.

## Next

Use the harness for a drag or connect/reconnect gesture fixture in JIH-040.
