# JNPE-040 - Conformance And Template Parent Expansion Traces

Date: 2026-06-02

Added adapter-facing coverage for parent expansion without changing fixture schema.

Changes:

- Added a runtime conformance runner scenario for `ApplyNodeDrag` producing `set_node_pos` and
  `set_group_rect`.
- Added an adapter conformance scenario for the same parent expansion trace and callback counts.
- Added a fifth headless adapter template smoke scenario, `template node drag parent expansion`.

Decision:

- No new conformance action is needed. `ConformanceAction::ApplyNodeDrag` already targets the
  runtime interaction boundary an adapter should call.

Validation:

- `cargo fmt --check` passed.
- `cargo nextest run -p jellyflow-runtime conformance` passed, 53 tests run.
- `cargo nextest run -p jellyflow-runtime adapter_conformance` passed, 17 tests run.
- `cargo test --manifest-path templates/headless-adapter/Cargo.toml` passed, 8 tests run.
- `cargo run --manifest-path templates/headless-adapter/Cargo.toml -- check` passed, 5 matching
  scenarios.

Next task: JNPE-050, documentation and closeout.
