# JNPE-020 - Single-Parent Drag Expansion

Date: 2026-06-02

Implemented the minimal parent expansion planner slice for runtime node drag.

Changes:

- Added focused parent expansion tests under `runtime/tests/drag`.
- Added private `runtime::drag::parent_expansion`.
- Extended drag candidates with parent group id and effective `expand_parent` policy.
- Appended `SetGroupRect` ops after node movement ops when a moved child exceeds its parent group
  rect.

Validation:

- Red check: `cargo nextest run -p jellyflow-runtime drag_parent_expansion` failed before
  implementation because expansion emitted only `SetNodePos`.
- `cargo fmt --check` passed.
- `cargo nextest run -p jellyflow-runtime drag_parent_expansion` passed, 2 tests run.
- `cargo nextest run -p jellyflow-runtime drag` passed, 40 tests run.
- `cargo clippy -p jellyflow-runtime --all-targets -- -D warnings` passed.

Next task: JNPE-030, multi-parent ordering and left/top sibling/coordinate behavior.
