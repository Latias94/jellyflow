# JVE-030 - Conformance And Template Coverage

Date: 2026-06-02

Added visible node id assertions to the conformance/template harness.

Result:

- Added `ConformanceAction::AssertVisibleNodeIds`.
- Added runner comparison against `NodeGraphStore::visible_node_ids(viewport_size)`.
- Added runtime conformance and adapter-conformance tests.
- Added a visible-node scenario to the headless adapter template suite.
- Updated template scenario counts from 7 to 8.

Validation:

- `cargo fmt --check`
- `cargo nextest run -p jellyflow-runtime conformance`
- `cargo nextest run -p jellyflow-runtime adapter_conformance`
- `cargo nextest run -p jellyflow-runtime --test public_surface`
- `cargo test --manifest-path templates/headless-adapter/Cargo.toml`
- `cargo run --manifest-path templates/headless-adapter/Cargo.toml -- check`

Next:

- JVE-040 should update docs, run package closeout gates, and keep visible edge ids plus real
  spatial indexing as follow-ons.
