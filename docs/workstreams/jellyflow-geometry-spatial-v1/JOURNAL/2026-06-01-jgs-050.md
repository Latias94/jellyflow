# JGS-050 Edge Path And Hit Test

Status: DONE
Date: 2026-06-01

## Summary

Added backend-neutral edge path and hit-test primitives to `runtime::geometry`.

New path surface:

- `PathCommand`
- `EdgePath`
- `EdgePathLabel`
- `BezierEdgeOptions`
- `SmoothStepEdgeOptions`
- `straight_edge_path`
- `bezier_edge_path`
- `smoothstep_edge_path`

New hit-test surface:

- `EdgeHitTestOptions`
- `edge_path_distance`
- `edge_path_contains_point`
- `NodeGraphInteractionState::edge_hit_test_options`

The canonical output is Rust data: move/line/cubic commands, labels, and numeric distances. SVG
strings remain an adapter concern.

## Validation

- `cargo fmt --check`: passed.
- `cargo nextest run -p jellyflow-runtime edge_path`: passed, 4 tests.
- `cargo nextest run -p jellyflow-runtime hit_test`: passed, 3 tests.
- `cargo check -p jellyflow-runtime`: passed.

## Review

Self-review against the `review-workstream` checklist found no blocking or important findings.
The smoothstep path is intentionally "smoothstep-like": deterministic orthogonal command data
without encoding SVG-specific rounded-corner syntax.

## Next

Proceed to JGS-060: document the shipped public geometry surface and run package-level gates.
