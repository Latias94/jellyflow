# JPB-030 - Runtime Policy Facade

Date: 2026-05-30

## Changes

- Added `crates/jellyflow-runtime/src/runtime/policy.rs`.
- Published read-only helpers for effective node, port, and edge interaction policy resolution.
- Added focused tests for global defaults, per-element overrides, disabled precedence, and
  endpoint-specific edge reconnectability.
- Exposed the module through `jellyflow_runtime::runtime::policy`.

## Evidence

- `cargo check -p jellyflow-runtime`: passed.
- `cargo nextest run -p jellyflow-runtime policy`: passed with 5 selected tests and 65 skipped.
- `cargo clippy -p jellyflow-runtime --all-targets -- -D warnings`: passed.
- `review-workstream` self-review: no blocking findings.
- `verify-rust-workstream`: task-local claim verified.

## Next Task

JPB-040: route intended connect/delete/reconnect checks through `runtime::policy`, splitting
behavior enforcement if the change is broader than expected.
