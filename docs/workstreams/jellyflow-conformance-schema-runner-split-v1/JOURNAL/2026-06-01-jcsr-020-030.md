# JCSR-020/JCSR-030 Journal

Date: 2026-06-01

## Work

- Replaced `crates/jellyflow-runtime/src/runtime/conformance/scenario.rs` with
  `scenario/mod.rs` plus `constants`, `suite`, `setup`, `action`, and `trace` submodules.
- Replaced `crates/jellyflow-runtime/src/runtime/conformance/runner.rs` with `runner/mod.rs` plus
  `actions`, `trace`, and `callbacks` submodules.
- Preserved public `jellyflow_runtime::runtime::conformance::*` re-exports.
- Preserved fixture schema version, serde tags, serde defaults, trace event ordering, callback
  payloads, and runner error strings.

## Validation

- `cargo check -p jellyflow-runtime --all-targets`: pass.
- `cargo fmt --check`: pass.
- `cargo nextest run -p jellyflow-runtime conformance`: pass, 26 tests.
- `cargo nextest run -p jellyflow-runtime --example conformance_harness`: pass, 3 tests.
- `cargo nextest run -p jellyflow-runtime --test public_surface`: pass, 3 tests.
- `cargo clippy -p jellyflow-runtime --all-targets -- -D warnings`: pass.
- `cargo nextest run -p jellyflow-runtime`: pass, 177 tests.

## Review Notes

The split is private and mechanical. No fixture schema, public path, new gesture action, adapter,
renderer, screenshot, pixel, or platform dependency was introduced.
