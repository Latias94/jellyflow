# JCCH-020 Journal

Date: 2026-06-01

## Scope

Implemented a renderer-free example harness for conformance fixture directory check/approve flows.

## Changes

- Added `crates/jellyflow-runtime/examples/conformance_harness.rs`.
- Added `check <fixture-dir>` mode.
- Added `approve <fixture-dir>` mode.
- Added pretty JSON report output.
- Added example-local tests for stale check failure, approve write-back plus passing check, and
  usage errors.

## Validation

- `cargo nextest run -p jellyflow-runtime --example conformance_harness`: passed, 3 tests.
  Run ID: `a971fa87-eeab-4da4-bfde-c412905601e8`.
- `cargo check -p jellyflow-runtime --examples`: passed.

## Notes

The harness remains an example binary. A dedicated CLI crate is a follow-on only if more commands
accumulate.
