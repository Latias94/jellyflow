# JCFD-020 Journal

Date: 2026-06-01

## Scope

Implemented deterministic recursive JSON discovery for conformance fixture directories.

## Changes

- Added `ConformanceFixtureDirectory` as the public directory-level fixture loader.
- Added `ConformanceSuiteFile` so each loaded suite retains its source path.
- Added path-aware directory and suite-file reports.
- Derived serde traits for directory fixture and report types.
- Extended `ConformanceFixtureFileError` with directory traversal variants.
- Added focused tests and public-surface coverage.

## Validation

- `cargo nextest run -p jellyflow-runtime conformance_fixture_directory`: passed, 3 tests.
  Run ID: `b8fd5eb5-cc74-456f-92ee-2ae877ee6a35`.
- `cargo nextest run -p jellyflow-runtime --test public_surface`: passed, 3 tests.
  Run ID: `02f40c26-70bf-4d41-a0c0-c91888787616`.
- `cargo check -p jellyflow-runtime`: passed.

## Notes

Golden approval/update behavior remains a follow-on. Renderer assets stay outside
`jellyflow-runtime`.
