# JDC-020 - Template Delete Smoke

Date: 2026-06-02

Completed template delete selection smoke coverage.

Result:

- Added a `template delete selection` conformance scenario to the built-in headless adapter suite.
- Added `run_delete_selection_smoke` for single-scenario template testing.
- Added explicit `keyboard-types` dependency to the external template so it can model normalized
  key-code input.
- Updated template suite counts from 6 to 7 scenarios.

Validation:

- `cargo fmt --check`
- `cargo nextest run -p jellyflow-runtime conformance`
- `cargo nextest run -p jellyflow-runtime adapter_conformance`
- `cargo test --manifest-path templates/headless-adapter/Cargo.toml`
- `cargo run --manifest-path templates/headless-adapter/Cargo.toml -- check`

Next:

- JDC-030 documentation and closeout.
