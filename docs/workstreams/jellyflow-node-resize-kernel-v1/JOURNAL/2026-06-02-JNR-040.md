# JNR-040 - Conformance And Template Resize Coverage

Date: 2026-06-02

Added conformance vocabulary and template coverage for the stable target-size resize planner.

Result:

- Added `apply_node_resize` to `ConformanceAction`.
- Added serde-friendly resize request, constraints, and direction wrappers.
- Routed conformance execution through `NodeGraphStore::apply_node_resize`.
- Added runner coverage for size-only resize traces.
- Added adapter conformance coverage for direction-aware resize traces.
- Added `template node resize` to the headless adapter smoke suite.

Validation:

- `cargo fmt --check`
- `cargo nextest run -p jellyflow-runtime conformance`
- `cargo nextest run -p jellyflow-runtime adapter_conformance`
- `cargo test --manifest-path templates/headless-adapter/Cargo.toml`
- `cargo run --manifest-path templates/headless-adapter/Cargo.toml -- check`
- `cargo clippy -p jellyflow-runtime --all-targets -- -D warnings`

Next task: JNR-050, documentation and closeout.
