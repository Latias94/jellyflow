# JNDMS-020 Journal

Date: 2026-06-01

## Work

- Replaced the single `crates/jellyflow-runtime/src/runtime/drag.rs` file with
  `crates/jellyflow-runtime/src/runtime/drag/mod.rs`.
- Split public drag types, pure planning, candidate discovery, constraint math, and store extension
  methods into owned submodules.
- Preserved the public `jellyflow_runtime::runtime::drag::*` paths through facade re-exports.

## Validation

- `cargo check -p jellyflow-runtime --all-targets`: pass.
- `cargo fmt --check`: pass.
- `cargo nextest run -p jellyflow-runtime drag`: pass, 10 tests.
- `cargo nextest run -p jellyflow-runtime conformance`: pass, 26 tests.
- `cargo nextest run -p jellyflow-runtime --test public_surface`: pass, 3 tests.
- `cargo clippy -p jellyflow-runtime --all-targets -- -D warnings`: pass.
- `cargo nextest run -p jellyflow-runtime`: pass, 177 tests.

## Review Notes

The split is mechanical. The facade owns all public re-exports, while private helpers remain
`pub(super)` or private. The lane did not change parent expansion, resize, pointer capture,
adapter, renderer, or fixture schema behavior.
