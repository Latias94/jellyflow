# JCF-040 - Convert Existing Scenarios

Status: DONE
Date: 2026-06-01

## Summary

Converted the connect and node drag adapter-conformance trace scenarios from the private
interaction harness to the public conformance fixture runner.

## Implementation Notes

- Connect dispatch now asserts graph-commit and XyFlow connection callback trace through
  `run_conformance_scenario`.
- Connect gesture lifecycle and connect gesture transaction callbacks now execute as fixture
  actions.
- Node drag gesture transaction callbacks now execute as fixture actions using `apply_node_drag`.
- Reconnect, delete, viewport, and geometry tests remain as focused private harness/direct tests
  because they are outside this conversion slice.

## Validation

- `cargo nextest run -p jellyflow-runtime adapter_conformance`: PASS, 8 tests.
- `cargo nextest run -p jellyflow-runtime conformance`: PASS, 11 tests.
- `cargo check -p jellyflow-runtime`: PASS.

## Next

JCF-050 should document the fixture strategy in README material, run broad closeout gates, and close
the lane or split follow-ons.
