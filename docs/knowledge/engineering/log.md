# Engineering Memory Update Log

## 2026-07-04
* **Open GPUI product polish/layout-pass implementation**: Executed `docs/plans/2026-07-03-001-feat-open-gpui-product-polish-layout-pass-plan.md` through U7. The Jellyflow repo now has stricter widget-free adapter gates for explicit measured readable/control/drag-exclusion/overflow evidence, dense inspector/blackboard/menu interaction coverage, product visual reports, graph affordance evidence, and native visual/interaction regressions. The local `repo-ref/open-gpui` main fork has the matching `canvas-jellyflow` consumer changes: host-local component atoms, Dify/shader/ERD/mind-map product renderer polish, dense surface panels, dropped-wire/node-menu/inspector/blackboard probes, native drag/close smoke, screenshot ROI smoke, and projection-only degradation checks.
* **Projection fallback boundary hardening**: Removed the last visual-regression path that inferred drag-exclusion evidence from renderer presets, measured control counts, or projected controls. Product visual reports now require explicit Open GPUI `drag_exclusion` measured regions; projection-only rows carry `MeasuredInternalsEvidenceIncomplete` plus `MeasuredInternalsProjectionFallback` and cannot satisfy product-ready layout proof.
* **Focused U7 verification**: Re-ran root and Open GPUI example format, `jellyflow-open-gpui` explicit drag-exclusion evidence nextest, focused product surface report tests, and the Open GPUI example U7 smoke set covering native launch/drag/last-window-close, product fixture non-overlap, measured port hotspot reachability, reconnect sequence, dropped-wire gesture, and projection-only drag-exclusion rejection. All passed; existing Open GPUI `block v0.1.6` future-incompat warning noise remains out of scope.
* **U8 documentation boundary**: Refreshed `jellyflow-open-gpui` docs and engineering memory around the final maturity split: runtime owns headless semantics; `jellyflow-open-gpui` owns widget-free adapter plans/reports/gates; `canvas-jellyflow` owns concrete Open GPUI widgets, component atoms, measured-element collection, focus/pointer arbitration, screenshots, and native lifecycle smoke. egui and Dioxus remain semantic-contract targets rather than mature adapter peers for this Open GPUI stage.

## 2026-07-03
* **Open GPUI product polish/layout-pass plan**: Created `docs/plans/2026-07-03-001-feat-open-gpui-product-polish-layout-pass-plan.md` as the next implementation-ready plan. It keeps runtime headless and `jellyflow-open-gpui` widget-free, demotes projection fallback from product proof to degraded evidence, uses Open GPUI host-local component atoms, and scopes the next fearless-refactor stage to Dify/shader/ERD/mind-map polish, inspector/blackboard/menu surfaces, real measured readable/control/overflow regions, dynamic repeatable ports, and stronger native/visual gates.
* **Open GPUI component-host seam U4/U5 implementation**: Added stable readable, drag-exclusion, and overflow measurement ids in `jellyflow-open-gpui`; added host-local `OpenGpuiNodeSizePolicy` in `canvas-jellyflow`; projected product nodes from semantic size budgets instead of arbitrary text fitting; wrapped controls with measured drag-exclusion regions; converted repeatable overflow badges into component-declared measured overflow regions; and removed the character-count overflow badge heuristic. The mind-map fixture now spaces idea nodes for the intrinsic topic/source card budgets instead of relying on old compact sizes.
* **Open GPUI component-host seam U4/U5 verification**: Re-ran `cargo fmt --all`, `cargo fmt --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml`, `git diff --check`, `git -C repo-ref/open-gpui diff --check`, focused product report/resize/non-overlap tests, `cargo nextest run -p jellyflow-open-gpui --no-fail-fast` (101 tests), `cargo nextest run -p jellyflow-runtime --no-fail-fast` (471 tests), and `cargo nextest run --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml -p open-gpui-canvas-jellyflow --no-fail-fast` (65 tests). All passed; existing Open GPUI `block v0.1.6` future-incompat warning noise remains out of scope.
* **Open GPUI component-host seam U6-U8 closeout**: Confirmed the remaining plan boundary with existing evidence rather than adding new host hacks. Dify-style nodes already project inspector plans, dropped-wire insertion uses semantic action/menu planning, shader blackboards dispatch through action plans, and connect/reconnect remains a continuous canvas gesture plus connection planner. There is no current builtin Graph-target action, so the graph-menu portion is intentionally not materialized as an empty menu. The 006 seam is now the baseline for future Open GPUI polish.
* **Open GPUI component-host seam U1-U3 implementation**: Implemented the first slice of `docs/plans/2026-07-02-006-refactor-open-gpui-component-host-seam-plan.md` in `repo-ref/open-gpui/examples/canvas-jellyflow`. The label-only `OpenGpuiNodeComponentRegistry` was deleted, product renderers now use the existing widget-free renderer registry plus one concrete host render table, `render_open_gpui_node_wrapper` gives custom and fallback renderers one wrapper path, component context exposes semantic props/services/actions/internals invalidation, and repeatable/static anchors use measured handle/anchor helpers instead of hidden/offscreen hacks.
* **Open GPUI component-host seam U1-U3 verification**: Re-ran `cargo fmt --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml -- --check`, `cargo check --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml`, `cargo nextest run -p jellyflow-open-gpui --no-fail-fast` (101 tests), `cargo nextest run --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml -p open-gpui-canvas-jellyflow --no-fail-fast` (64 tests), and `git -C repo-ref/open-gpui diff --check`. Existing Open GPUI macOS warning noise remains out of scope.

## 2026-07-02
* **Open GPUI product-gallery UX plan**: Created `docs/plans/2026-07-02-001-fix-open-gpui-product-gallery-ux-plan.md` from native review feedback covering text clipping, box-select offset, shader/ERD dragging, resize top-sticking, reprojection resets, and mind-map initial overlap.
* **Subagent findings consolidated**: Read-only probes agreed on three root causes: fixed product-card sizes plus absolute rows hid unreadable content, the example used inferred viewport/frame data instead of actual canvas bounds, and node overlay event shielding did not distinguish interactive controls from drag surfaces.
* **Gallery UX implementation slice**: Tightened `jellyflow-open-gpui` visual gates around `content_readable` and node overlap, added adapter-local readable min-size budgets in the Open GPUI example, exposed actual canvas `Bounds<Pixels>` to the example, split control event shielding from drag surfaces, synced canvas transforms back into Jellyflow before reprojection, and spaced shader/ERD/mind-map fixtures against readable node budgets.
* **Verification caveat**: Formatting, diff checks, root `cargo check`, Open GPUI `canvas-jellyflow` check, and the unreadable-content visual gate regression pass. Local `cargo test` binaries for runtime and the Open GPUI example can still hang after startup in this environment, so this remains the next verification issue before claiming the full plan gate green.
* **Open GPUI product-interactions planning**: Ran read-only subagent audits for shader/connection input, component UI polish, adapter productization, and `egui-snarl` comparison after native review still found shader drag, port/edge clickability, reconnect, and component readability gaps. Created `docs/plans/2026-07-02-002-feat-open-gpui-product-interactions-plan.md` as the next implementation-ready fearless-refactor plan. The plan keeps runtime headless and focuses the next stage on Open GPUI overlay pointer routing, visible Select/Pan/Connect tooling, port hotspot/reconnect UX, widget-free adapter presets/reports/sync planners, and polished Dify/Shader/ERD/MindMap renderers.
* **Open GPUI product-interactions implementation**: Executed U1-U7 in dependency order. Runtime builtin kits now publish semantic readable layout budgets; `jellyflow-open-gpui` owns widget-free product presets, style budgets, host reports, measurement revision decisions, transform sync, and batch connection/reconnect sync planners; `canvas-jellyflow` owns the concrete Open GPUI product renderers, event routing, visible tooling, port/reconnect affordances, dropped-wire gesture path, and gallery UI.
* **Open GPUI product-interactions U8 hardening**: Promoted the final product interaction baseline into `jellyflow-open-gpui::testing::assert_product_interaction_report_gates`, so future hosts fail if full drag pointer sequences, control shielding, port hotspot reachability, Select/Pan/Connect tooling, connect/reconnect/dropped-wire store sync, or repeatable overflow indicators regress. Updated the node UI authoring regression docs and current-state memory to distinguish hard structured gates from screenshot/manual review aids.
* **Open GPUI drag-state regression fix**: Native review found product nodes could move a little and then stop. The example was syncing canvas transforms into `NodeGraphStore` on pointer move and immediately rebuilding `CanvasEditor`, which dropped the active translating state. `canvas-jellyflow` now defers editor refresh while pointer interactions are active, then refreshes after idle; a real `JellyflowCanvasView` regression test covers two consecutive product-surface moves.
* **Open GPUI canvas/node UI foundations plan**: Created `docs/plans/2026-07-02-003-feat-open-gpui-canvas-node-ui-foundations-plan.md` after comparing Jellyflow's current Open GPUI path with egui-snarl-style viewer ergonomics, existing Open GPUI canvas route/hit-test primitives, and React Flow-style custom node/handle affordances. The plan narrows the next fearless-refactor stage to Open GPUI product quality: shared committed/preview wire policies, pointer capture and drag-region policy, port/reconnect/drop gestures, widget-free adapter evidence, host-local adaptive node layout, Dify/Shader/ERD/MindMap renderer rework, and hard regression gates.
* **Open GPUI canvas/node UI foundations U1-U4**: Completed the first canvas interaction foundation slices in `repo-ref/open-gpui`. The generic canvas now has route-consistent connection/reconnect previews, drag threshold/capture stabilization, connection release events for connected/reconnected/dropped/rejected outcomes, wider port and reconnect hit targets, valid/invalid/free target feedback in paint frames, and `canvas-jellyflow` consumes dropped releases for Dify-style dropped-wire insertion instead of guessing from raw pointer-up state.
* **U4 verification**: Re-ran open-gpui diff/format checks, focused connect/reconnect/preview tests, full `repo-ref/open-gpui/crates/canvas` tests (363 unit tests, 2 json-canvas tests, 8 spatial-index tests, and doc-tests), `canvas-jellyflow` dropped-wire tests with `open_gpui_platform/runtime_shaders`, and `canvas-jellyflow` check with `runtime_shaders`; all passed. Existing `gpui_macos` check-cfg warnings and the example non-test `GraphOp` / `product_tool_switcher_visible` warnings remain out of scope.
* **Canvas foundations U5 adapter evidence**: Added widget-free graph affordance evidence to `jellyflow-open-gpui` so product reports now require route families, committed/preview route parity, port/reconnect hit budgets, drag regions, and readable layout-region evidence. Committed as `0982b61 feat(open-gpui): gate graph affordance evidence`; `cargo nextest run -p jellyflow-open-gpui --no-fail-fast` passed with 90 tests.
* **Canvas foundations U6 adaptive layout primitives**: Added host-local `AdaptiveNodeLayoutStack`, region modes, and repeatable-list planning in `canvas-jellyflow` so Open GPUI product nodes can degrade full -> compact -> shell before overflowing fixed node bounds. Committed locally in `repo-ref/open-gpui` as `3c30e48 feat(canvas): add adaptive node layout primitives`.
* **Canvas foundations U7 product renderer rewrite**: Reworked the Dify, shader, ERD, topic, and source renderers around dynamic product layout plans instead of fixed row constants; reduced-size layout tests now assert regions stay inside cards and repeatable overflow reserves indicator height. Committed locally in `repo-ref/open-gpui` as `1e9618b feat(canvas): adapt product renderers to layout plans`; full `canvas-jellyflow` product tests with `open_gpui_platform/runtime_shaders` passed with 54 tests.
* **Canvas foundations U8/U9 docs and regression closeout**: Updated the `jellyflow-open-gpui` README, node UI regression docs, component-contract decision, Open GPUI component-kit decision, current-state memory, and this log with the final boundary: generic canvas graph UX in Open GPUI canvas, widget-free adapter presets/reports in `jellyflow-open-gpui`, and concrete adaptive component layout in `canvas-jellyflow`.
* **Canvas foundations final verification**: Re-ran root/open-gpui format and diff checks, `cargo nextest run -p jellyflow-open-gpui --no-fail-fast` (90 tests), `cargo nextest run -p jellyflow-runtime -p jellyflow-egui -p jellyflow-proof --lib --no-fail-fast` (532 tests), `cargo test -p jellyflow-runtime --test public_surface -- --nocapture` (3 tests), full `canvas-jellyflow` tests with `open_gpui_platform/runtime_shaders` (54 tests), and full `repo-ref/open-gpui/crates/canvas` lib tests (363 tests); all automated gates passed. Native launch smoke reached the Open GPUI app event loop and was interrupted manually; `canvas-jellyflow` sets `QuitMode::LastWindowClosed`, but last-window-close exit was not GUI-automated in this terminal session. Existing `gpui_macos` warning noise remains out of scope.
* **Open GPUI native UX polish plan**: Created `docs/plans/2026-07-02-004-feat-open-gpui-native-ux-polish-plan.md` as the next implementation-ready plan. The plan narrows the next fearless-refactor stage to Open GPUI native product quality: automated last-window-close evidence, screenshot region evidence, route/port/reconnect polish, full reconnect pointer sequences, node drag regression coverage, component text-fit/readability, and widget-free `jellyflow-open-gpui` gates.
* **Native UX polish plan doc review**: Ran the mandatory markdown planning review inline because no subagents were opened in this turn. The review applied coverage fixes for node drag regression evidence, configured route-shape fallback checks, and the deferred boundary for full Open GPUI layout-pass bound harvesting; static markdown/path/whitespace checks passed.
* **Native UX polish U1-U4**: Implemented structured native lifecycle evidence, screenshot ROI evidence, generic wire/reconnect visual states, and full reconnect endpoint sequence evidence. `jellyflow-open-gpui` now rejects blank native windows, skipped close automation, missing screenshot regions, direct-line preview fallback, missing graph affordance evidence, and incomplete reconnect source/target/rollback/drop/retry sequences. Matching Open GPUI local commits landed in `repo-ref/open-gpui` through `2773dcd`.
* **Native UX polish U5**: Tightened Open GPUI product-node internals with host-local adaptive text/control plans, preserved full/compact/shell region modes in renderers, width-aware shader repeatable limits, ERD overflow badges, and a first pass at widget-free fit-estimation evidence. That direction was later superseded by the XYFlow-style measured-internals refactor because adapter gates must not guess arbitrary text/control fit.
* **Native UX polish U6**: Consolidated native UX claims into adapter-owned hard gates, including the now-deprecated fit-estimation budget/evidence path. The later 005 refactor replaces that path with measured internals evidence while keeping native lifecycle, screenshot ROI, product interaction, and reconnect sequence evidence.
* **Native UX polish focused verification**: Re-ran `cargo nextest run -p jellyflow-open-gpui --no-fail-fast` (100 tests) and full `canvas-jellyflow` tests with `open_gpui_platform/runtime_shaders` (65 tests); both passed. Earlier U4 verification also passed full `repo-ref/open-gpui/crates/canvas` lib tests. Existing Open GPUI macOS warning noise remains out of scope.
* **Native UX polish final verification**: Closed U7 documentation/memory and re-ran the broad final gates after the last visual-gate assertion alignment: root/open-gpui format and diff checks, `jellyflow-open-gpui` nextest (100 tests), runtime/egui/proof lib nextest (532 tests), runtime public-surface tests (3 tests), full `repo-ref/open-gpui/crates/canvas` lib tests (366 tests), and full `canvas-jellyflow` product tests with `open_gpui_platform/runtime_shaders` (65 tests). `repo-ref/open-gpui` remains local `main` ahead of origin and was not pushed.
* **XYFlow-style node component refactor plan**: Created `docs/plans/2026-07-02-005-refactor-open-gpui-xyflow-node-components-plan.md` after user review rejected heuristic fit contracts as library abstractions. The plan supersedes the deprecated fit-estimation budget/evidence direction with a cleaner Open GPUI-first boundary: host-registered node components, component-owned handles, measured node internals, explicit internals-update requests, graph-owned connection/reconnect UX, and deletion or demotion of arbitrary text/control fit heuristics.
* **XYFlow-style node component refactor implementation**: Removed the public Open GPUI adapter fit-estimation budget/export path, replaced visual gate fit evidence with `OpenGpuiMeasuredInternalsEvidence`, added an example-local `OpenGpuiNodeComponentRegistry`/component context seam, deleted the old host text/control fit helpers from `canvas-jellyflow`, and narrowed public style evidence to graph-owned affordance numbers. Product reports now prove canvas/document bounds, handle coverage, readable regions, drag-exclusion coverage, stale state, and component-declared overflow instead of estimating arbitrary text/control fit.
* **XYFlow-style node component refactor verification**: Re-ran the final gates after the public style-evidence cleanup: root and `canvas-jellyflow` format checks, `jellyflow-open-gpui` nextest (101 tests), runtime/egui/proof lib nextest (532 tests), runtime public-surface tests (3 tests), full `repo-ref/open-gpui/crates/canvas` lib tests (366 tests), full `canvas-jellyflow` product tests with `open_gpui_platform/runtime_shaders` (64 tests), and root/open-gpui diff whitespace checks. Existing Open GPUI macOS warning noise remains out of scope.
* **Open GPUI component-host seam research**: Cloned and reviewed additional reference projects under `repo-ref/` (`rete`, `baklavajs`, `dify`, `node-red`, `litegraph.js`, `egui_node_graph2`) alongside existing XYFlow, egui-snarl, and open-gpui sources. The durable conclusion is that Jellyflow already has the core semantic descriptors; the next missing layer is one Open GPUI host component seam that consolidates renderer registration, `NodeWrapper` props/context, component-owned measured handles, interaction regions, intrinsic sizing/overflow, and inspector/menu/blackboard routing.
* **Open GPUI component-host seam plan**: Created `docs/plans/2026-07-02-006-refactor-open-gpui-component-host-seam-plan.md` as the next implementation-ready plan. It keeps `jellyflow-runtime` semantic, keeps `jellyflow-open-gpui` widget-free, rejects a shared cross-framework widget crate, and starts the next fearless refactor with duplicate registry deletion, hidden-anchor/fallback cleanup, measured internals gates, and Dify/shader/ERD/mind-map fixtures all using one component host contract.

## 2026-06-19
* **Initialization**: Created engineering wiki memory bundle.
* **Decision capture**: Recorded the semantic-surface vs framework-adapter direction for Jellyflow UI.
* **ADR capture**: Added ADR 0008 to formalize the semantic surface boundary.
* **Plan capture**: Added a follow-up plan for semantic slots, rich node rendering, and selection UX.
* **Implementation slice**: Added runtime node surface slot descriptors, egui field-row slot rendering, decision-card rich rows, and partial-intersection box selection defaults.
* **Verification capture**: Recorded focused runtime/egui nextest checks, workspace check, public-surface checks, and gallery snapshot generation for automation-builder and ERD.
* **Second slice**: Extended egui semantic slots to badge, nested-region, and action-row rendering, with vertical action chips and larger sample node defaults.
* **Second verification**: Re-ran `cargo nextest run -p jellyflow-egui --no-fail-fast` and `cargo run -p jellyflow-egui --example gallery_snapshot`, then reviewed updated automation-builder and ERD snapshots.
* **Slot boundary refinement**: Treat `slot` as the data lookup path and keep `anchor` for adapter-local placement and port binding; legacy `field.*` rows still fall back to key tails.
* **Second-adapter proof**: Added a lightweight `jellyflow-proof` workspace crate that demonstrates a second adapter boundary without introducing a shared UI layer.
* **Session closeout**: Marked the second-adapter contract plan complete in workspace memory; the next useful track is adapter conformance around selection, hit regions, low-zoom degradation, and custom node UI semantics.
* **Verification closeout**: Confirmed the proof crate, runtime schema, and egui semantic-surface contract with `cargo fmt --all --check`, `cargo nextest run -p jellyflow-proof -p jellyflow-runtime -p jellyflow-egui --lib`, `cargo test -p jellyflow-proof`, `cargo test -p jellyflow-proof --example adapter_smoke`, and the targeted egui semantic-slot test.
* **Strategy clarification**: Confirmed the headless semantic-surface direction is correct; the next missing layer is an adapter/node-kit boundary for egui, Dioxus, gpui, and future frontends, not a shared widget crate.
* **Planning note**: Keep ADR 0008 as the durable architectural boundary for now; add a new ADR only if the adapter/node-kit boundary needs to be frozen after the next plan becomes concrete.
* **Node-kit design**: Defined node kits as versioned packages of semantic node families, surface recipes, default data, and fixtures, with adapters mapping those recipes to egui, Dioxus, or gpui locally.
* **Plan formalization**: Turned the node-kit boundary into `docs/plans/2026-06-19-003-feat-adapter-node-kit-boundary-plan.md`, with runtime schema manifest work, first kit families, egui cleanup, proof/template coverage, and docs updates split into implementation units.
* **Goal setup**: Created a goal to execute the node-kit plan end-to-end so the plan can be used as the active implementation target.
* **Implementation slice**: Reused `NodeKitRegistry::builtin()` in egui samples, proof, and the headless adapter template; added builtin kit fixture coverage for workflow, ERD, and mind-map; and fixed registry resolution so canonical kinds win over alias collisions.
* **Verification capture**: Confirmed `cargo fmt --all`, `cargo test -p jellyflow-runtime --lib schema::tests::kit -- --nocapture`, `cargo test -p jellyflow-egui`, `cargo test -p jellyflow-proof`, `cargo test -p jellyflow-proof --example adapter_smoke`, and `cargo test --manifest-path templates/headless-adapter/Cargo.toml` all passed.
* **Session capture**: Added a fresh handoff note for the node-kit implementation slice so the current state can survive compaction.
* **gpui substrate check**: Reviewed the `repo-ref/open-gpui` fork and confirmed its `canvas` crate already splits document/model, paint frame, painter, and widget overlay concerns, so Jellyflow can start a gpui proof from the existing substrate instead of forking a new canvas layer immediately.
* **gpui next step**: Treat `open-gpui` as the likely host for the next proof, and only plan library changes if the Jellyflow proof exposes missing slot-aware nested content or other semantic hooks that the current canvas substrate cannot express cleanly.
* **gpui overlay polish**: Fixed the `canvas-jellyflow` proof so semantic node content is rendered for every node instead of only feeling alive on selection, and added zoom-aware slot reduction plus `min_w(0)`/`min_h(0)`-style shrink constraints so rich node content stops overflowing blue nodes.
* **gpui verification**: Rechecked the example with `cargo check -p open-gpui-canvas-jellyflow` and `cargo nextest run -p open-gpui-canvas-jellyflow`; both passed after the layout refinement.

## 2026-06-20
* **gpui proof refresh**: Reworked `repo-ref/open-gpui/examples/canvas-jellyflow` to consume Jellyflow semantic descriptors from `NodeRegistry`/`NodeKitRegistry::builtin().node_registry()`, kept `CanvasKindRegistry` focused on renderer policy, and verified the example with `cargo check -p open-gpui-canvas-jellyflow`.
* **gpui continuity**: Added a fresh session handoff for the gpui proof so the retained-view adapter evidence survives compaction.
* **Density contract fix**: Corrected the `node_surface_projection_uses_layout_hints_for_density_and_slot_limits` test to assert the actual `display_label()`/data-preview contract for semantic slots, and cleaned up unused imports in the `open-gpui` jellyflow example.
* **Verification**: Re-ran `cargo nextest run -p jellyflow-runtime --lib` successfully; `cargo check --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml` also passed, but the broader workspace still emits pre-existing macOS `cargo-clippy` check-cfg warnings.

## 2026-06-23
* **gpui proof slot cap**: Added an adapter-local height-based slot cap in `repo-ref/open-gpui/examples/canvas-jellyflow` so full-density runtime projections do not visually overfill fixed-height gpui node surfaces.
* **gpui example verification**: Re-ran `cargo fmt --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml --check`, `RUSTFLAGS='-Awarnings' cargo test --quiet --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml --bin open-gpui-canvas-jellyflow`, and `RUSTFLAGS='-Awarnings' cargo check --quiet --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml`.
* **open-gpui sync attempt**: Fetched `repo-ref/open-gpui` from `origin/main`; `git pull --ff-only` could not merge because existing local `crates/gpui_docking` edits would be overwritten, so no stash, restore, reset, or checkout was used.
* **open-gpui sync resolved**: After confirming the local docking edits were an obsolete upstream intermediate state, the user approved discarding only `crates/gpui_docking`; `git restore -- crates/gpui_docking` followed by `git pull --ff-only` fast-forwarded open-gpui to `8d018ce`.
* **gpui proof cleanup**: Removed the stale `semantic_overlay` cache from `examples/canvas-jellyflow` so semantic node content is derived only from `NodeRegistry`/`NodeKitRegistry` at render time; reran format, check, tests, and a short launch smoke.

## 2026-06-29
* **open-gpui proof commit**: Committed the GPUI Jellyflow semantic node surface proof as `ce14140 feat(canvas): prove jellyflow semantic node surfaces` in `repo-ref/open-gpui`; the fork is now clean and ahead of origin by one commit.
* **ADR capture**: Added ADR 0009 to freeze node kits as versioned semantic packages with adapter-local mapping, deferring any shared widget crate until multiple adapters prove real reuse pressure.
* **Main repo verification**: Re-ran the main gate after ADR and memory updates: `cargo fmt --all --check`, `cargo nextest run -p jellyflow-runtime -p jellyflow-egui -p jellyflow-proof --lib` (469 tests), `cargo test -p jellyflow-proof --example adapter_smoke`, and `cargo test --manifest-path templates/headless-adapter/Cargo.toml` all passed.
* **Projection contract fix**: Moved field-row value projection into runtime so `surface_slots_projection` reads `data.fields[slot]` before ordinary JSON paths; this keeps GPUI, proof, and future Dioxus adapters on the same value lookup rule as egui.
* **Verification refresh**: After the projection fix, re-ran `cargo fmt --all --check`, `cargo nextest run -p jellyflow-runtime -p jellyflow-egui -p jellyflow-proof --lib` (469 tests), `cargo test -p jellyflow-proof --example adapter_smoke`, `cargo test --manifest-path templates/headless-adapter/Cargo.toml`, and `RUSTFLAGS='-Awarnings' cargo test --quiet --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml --bin open-gpui-canvas-jellyflow`; all passed.
* **Main repo commit**: Committed the node-kit/runtime/egui/proof/docs work as `feat(schema): add semantic node kit boundary` in the main Jellyflow repo.
* **open-gpui component proof**: Updated `repo-ref/open-gpui/examples/canvas-jellyflow` to depend on the new `open_gpui_ui_components` / `open_gpui_ui_core` crates and render semantic badge/action/preview slots with actual component-library widgets.
* **open-gpui handle-anchor fix**: Fixed the gpui Jellyflow example so projected canvas handles can follow `PortViewDescriptor.anchor` -> `NodeSurfaceSlotDescriptor.anchor` instead of always using evenly spaced port rows; also corrected the demo graph port keys to match the builtin node-kit descriptor keys (`out`, `prompt`, `completion`, `result`).
* **open-gpui verification**: Locked the handle-anchor regression with `projected_handles_follow_semantic_slot_anchors_after_node_resize`, then re-ran `cargo fmt --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml --check`, `RUSTFLAGS='-Awarnings' cargo test --quiet --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml --bin open-gpui-canvas-jellyflow`, `RUSTFLAGS='-Awarnings' cargo check --quiet --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml`, and a `cargo run` launch smoke.
* **UI parity planning**: Compared Jellyflow against `repo-ref/egui-snarl` and `repo-ref/xyflow`, then added `docs/plans/2026-06-29-001-feat-node-ui-capability-parity-plan.md` as the implementation-ready plan for reaching egui-snarl/XYFlow-class custom node UI without adding backend execution or a shared widget crate.
* **Runtime measurement first slice**: Implemented the U1/U2 base contract in `jellyflow-runtime`: node measurements now include revision, density, semantic slot rects, semantic anchor rects, fresh/dirty/missing status, adapter invalidation requests, and measurement-aware anchor fallback for edge endpoints and connection targets.
* **Runtime measurement verification**: Re-ran `cargo fmt --all`, `cargo check -p jellyflow-runtime`, `cargo nextest run -p jellyflow-runtime measurement`, and `cargo nextest run -p jellyflow-runtime explicit_modules_expose_their_owned_surfaces`; all passed.
* **Runtime resolver unification**: Started U3 by extracting a shared measurement-aware handle resolver for runtime layout facts and binding queries, so measured handles, measured semantic anchors, dirty-state fallback, and missing-measurement fallback no longer diverge across read models.
* **Runtime resolver verification**: Re-ran `cargo fmt --all -- --check`, `git diff --check`, `cargo check -p jellyflow-runtime`, `cargo nextest run -p jellyflow-runtime measurement binding store::lookups`, `cargo nextest run -p jellyflow-runtime explicit_modules_expose_their_owned_surfaces`, and `cargo nextest run -p jellyflow-runtime -p jellyflow-egui -p jellyflow-proof --lib`; all passed, with the broader nextest gate running 475 tests.
* **egui measurement publication**: Wired `JellyflowEguiBridge` snapshot reporting so rich `NodeInteractiveRegion` facts are published into runtime as measured semantic slots and measured semantic anchors, while keeping existing concrete handle bounds for compatibility.
* **egui measurement verification**: Added `erd_snapshot_reports_semantic_region_measurements_to_runtime`; reran the focused egui ERD tests and the broader `cargo nextest run -p jellyflow-runtime -p jellyflow-egui -p jellyflow-proof --lib` gate, now 476 tests passing.
* **GPUI measurement projection**: Updated `repo-ref/open-gpui/examples/canvas-jellyflow` so the retained proof builds projection-time runtime `NodeMeasurement` facts from semantic slots/anchors and projects `CanvasHandle` positions through `NodeGraphStore::resolve_node_handle_measurement`.
* **GPUI formula cleanup**: Deleted the obsolete `project_semantic_port_y` / `semantic_anchor_y` helper path from the GPUI example; semantic row math now exists only as measurement generation fallback until real component bounds are reported.
* **GPUI measurement verification**: Added `projected_handles_use_runtime_measurement_facts` and reran `cargo fmt --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml --check`, `RUSTFLAGS='-Awarnings' cargo test --quiet --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml --bin open-gpui-canvas-jellyflow`, and `RUSTFLAGS='-Awarnings' cargo check --quiet --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml`; all passed.
* **Main verification refresh**: Re-ran `cargo fmt --all -- --check`, `git diff --check`, `git -C repo-ref/open-gpui diff --check`, and `cargo nextest run -p jellyflow-runtime -p jellyflow-egui -p jellyflow-proof --lib`; the main nextest gate ran 476 tests successfully.
* **Connection lifecycle contract**: Added a U4 runtime connection lifecycle surface with hover valid/invalid/empty, committed, rejected, canceled, dropped-on-pane, and no-op states; `ConnectSessionOutcome` now exposes the lifecycle result while preserving existing gesture events.
* **Connection conformance coverage**: Added `AssertConnectionLifecycle` to adapter conformance actions and runner support, plus a fixture runner test that resolves a valid connection lifecycle without adapter UI dependencies.
* **U4 verification note**: `cargo fmt --all -- --check`, `git diff --check`, `git -C repo-ref/open-gpui diff --check`, `cargo check -p jellyflow-runtime`, `cargo check -p jellyflow-runtime --tests`, `cargo check -p jellyflow-egui -p jellyflow-runtime`, and open-gpui example check passed. Running the `jellyflow-runtime` libtest binary currently hangs after startup even for a single filtered test; `cargo test -p jellyflow-runtime --lib --no-run` succeeds and no hanging Jellyflow test processes were left running.

* **Edge route facts contract**: Implemented the U5 first slice: `EdgeViewDescriptor` now carries an optional `EdgeRouteKind`; runtime layout facts expose `LayoutEdgeRouteFacts` with resolved route kind, path, label anchor, hit-test width, and ephemeral interaction policy/selection facts.
* **Edge route adapter proof**: Updated egui snapshots to consume runtime `visible_edge_route_facts` for committed edge paths and edge hit testing, and updated the GPUI proof to map Jellyflow route hints into `open_gpui_canvas::CanvasEdgeRoute`.
* **Edge route conformance**: Extended layout-facts conformance expectations with route kind, hit width, and selected-state assertions; added a fixture runner test for adapter route facts.
* **U5 verification**: Re-ran `cargo fmt --all -- --check`, `cargo check -p jellyflow-core -p jellyflow-runtime -p jellyflow-egui --tests`, `cargo test -p jellyflow-runtime edge_route_facts -- --nocapture`, `cargo test -p jellyflow-runtime adapter_conformance_fixture_runner_asserts_edge_route_facts -- --nocapture`, `cargo test -p jellyflow-egui committed_edge_paths_use_runtime_route_facts -- --nocapture`, `RUSTFLAGS='-Awarnings' cargo test --quiet --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml --bin open-gpui-canvas-jellyflow projects_jellyflow_edge_route_hints_into_canvas_routes`, `RUSTFLAGS='-Awarnings' cargo check --quiet --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml`, `git diff --check`, and `git -C repo-ref/open-gpui diff --check`; all passed.
* **Node chrome contract**: Implemented U6 adapter-owned node chrome semantics: schema descriptors now cover resizer, toolbar, status strip, validation banner, run/action strip, and inspector anchor; runtime exposes `resolve_node_chrome_facts` for canvas-space placement and resize constraints without toolkit widgets.
* **Node chrome adapter proof**: Added Dify-style chrome to the builtin LLM recipe, wired egui to render/hit-test chrome facts while preserving legacy resize fallback, and updated the GPUI proof to project status/run/toolbar/resizer chrome into local Badge/Button overlay widgets.
* **U6 verification**: Re-ran `cargo fmt --all -- --check`, `cargo check -p jellyflow-core -p jellyflow-runtime -p jellyflow-egui --tests`, runtime node chrome/schema/kit focused tests, public-surface tests, egui chrome focused tests, and open-gpui canvas-jellyflow format/check/test; all passed, with the GPUI example now running 7 tests.
* **Semantic recipe expansion**: Completed U7 by adding status banners, port rails, config groups, and metric badges to runtime surface descriptors; expanded builtin node kits with shader/blueprint fixtures and richer workflow/ERD semantics while keeping slot data and anchors renderer-neutral.
* **egui reference hardening**: Completed U8 by making the field-list rich renderer slot-driven rather than field-only, preserving port-bearing row anchors before status/actions, adding a shader graph sample/example, and verifying measured config/preview/port-rail rendering without field rows.
* **GPUI retained proof hardening**: Completed U9 by projecting the shader node-kit fixture through the open-gpui canvas-jellyflow example, registering `shader-card`, handling the new slot kinds, and locking the semantic slot-anchor handle behavior with GPUI tests.
* **Dioxus-shaped proof**: Completed U10 by adding a framework-neutral component-tree proof in `jellyflow-proof` that consumes the same builtin kit descriptors and measured child bounds without introducing Dioxus/widget types into runtime/core.
* **Example gallery and docs**: Completed U11/U12 documentation by adding `docs/examples/README.md`, documenting Dify-style workflow, shader/blueprint, ERD, and knowledge-canvas coverage, and updating README/template adapter guidance with custom-node authoring and adapter responsibility checklists.
* **U7-U12 focused verification**: Re-ran focused runtime kit/fixture tests, egui renderer/sample tests, proof crate tests plus `adapter_smoke`, template conformance tests, and GPUI canvas-jellyflow format/check/test; the GPUI example now runs 8 tests. A final broad verification pass is still in progress before closing the plan goal.
* **Visual confidence fix**: Re-ran egui gallery snapshots and fixed low-zoom Dify/automation node overflow by making rich node drawing degrade by zoom plus screen size, while preserving measured field/port anchors for connections.
* **Final plan verification**: Completed the full node UI parity verification contract: main fmt/diff checks, runtime nextest (451 tests), egui nextest (41 tests), proof nextest (4 tests), runtime public-surface tests, proof adapter smoke, headless adapter template tests, egui examples check, GPUI canvas-jellyflow format/check/test, and egui gallery snapshot generation all passed.
* **Completion audit**: Matched the plan Definition of Done against current evidence: runtime measurement/dynamic internals/connection lifecycle are framework-free; egui and GPUI prove measured internals; proof/template consume the same contracts; Dify, shader/blueprint, ERD, and knowledge examples exist; geometry resolves through measured facts; docs keep backend execution out of scope; and old GPUI formula/overlay paths were not found in the final search.

## 2026-06-30
* **Review-fix goal setup**: Created a follow-up goal to address review findings around dynamic internals invalidation, GPUI measurement proof, shader typed-port feedback, proof coverage, and mature node UI kit design research.
* **Subagent research capture**: Read-only subagents compared Jellyflow against xyflow, egui-snarl, Rete, Unreal Blueprint, Unity Shader Graph, Dify, and mind-map products. The durable conclusion remains `headless semantic surface + node kits + adapter-local widgets`, with a new emphasis on a Node UI Kit Contract, adapter capability matrix, repeatable field/control descriptors, action/menu descriptors, diagnostics bound to slots/ports/chrome, and conformance fixtures.
* **Adapter internals controller**: Added the headless `NodeInternalsController` facade so adapters have a short-lived, correct path for invalidating, reporting, and checking node-internal geometry without duplicating update-node-internals logic.
* **egui remeasurement fix**: Moved egui snapshot rebuilding to report current-frame measured handles/slots/anchors before querying layout facts, and locked data, resize, and zoom invalidation with focused node-internals tests.
* **Shader typed-port feedback**: Wired egui connection target resolution and commit through the same typed planner so shader incompatible hover feedback and commit rejection agree; added a shader graph regression for `vec4 -> float` invalid connection.
* **GPUI proof cleanup**: Kept the open-gpui example's `NodeSurfaceComponentLayout` as the single local component-region model shared by rendering and runtime measurement projection, and added a regression proving measured slot/anchor rects come from that shared layout.
* **Proof coverage**: Extended `jellyflow-proof` integration tests with dynamic component child remeasurement, proving changed child sets update runtime measurement slots/anchors and remain fresh in layout facts.
* **Focused verification**: Re-ran `cargo check -p jellyflow-egui --lib`, egui node-internals/product-sample/shader typed-port tests, GPUI canvas-jellyflow measurement tests, and `cargo test -p jellyflow-proof --test proof -- --nocapture`; all passed.
* **Node UI Kit Component Contract**: Captured the durable contract for rich custom nodes: runtime owns semantic slots, anchors, typed feedback, revisioned internals measurement, and conformance; adapters own toolkit widgets, real layout reporting, menus, inspector state, and visual regression.
* **Review-fix verification refresh**: Re-ran `cargo test -p jellyflow-runtime invalidating_node_internals_marks_measurement_dirty_without_mutating_graph --lib`, `cargo test -p jellyflow-egui data_change_notification_invalidates_node_internals_until_remeasured --lib`, `cargo test -p jellyflow-proof proof_component_tree_measurements_can_drive_runtime_layout_facts`, and full `open-gpui` canvas-jellyflow example tests; all passed.
* **Broad review-fix verification**: Re-ran main and GPUI format/diff checks, `cargo nextest run -p jellyflow-runtime -p jellyflow-egui -p jellyflow-proof --lib --no-fail-fast` (505 tests), `cargo test -p jellyflow-proof --test proof -- --nocapture` (3 tests), `cargo test --manifest-path templates/headless-adapter/Cargo.toml` (19 tests), `cargo check -p jellyflow-egui --examples`, and GPUI canvas-jellyflow check/test (9 tests); all passed. The remaining product gap is now design/system polish around adapter-local component registries, menus/actions, and visual regression rather than the core headless measurement/typed-port contract.
* **Authoring-contract plan**: Used `ce-plan` plus read-only subagent research to create `docs/plans/2026-06-30-001-feat-node-ui-authoring-contracts-plan.md` as the canonical implementation-ready roadmap for the remaining UI authoring gap: adapter capability levels, Field/Control Descriptor, repeatable collections, diagnostics, action/menu/inspector/blackboard contracts, egui authoring renderer, GPUI layout-pass measurement proof, proof/template updates, and visual/interaction regression. A duplicate draft plan was removed, external prior-art links were verified with HTTP 200 responses, and `git diff --check` passed for the document changes.
* **Authoring-contract implementation**: Completed U1-U8 for the Node UI Authoring Contracts plan. Runtime now has adapter capability requirements/gaps, headless Field/Control Descriptor, repeatable collections with stable item slot/anchor projection, action/menu/inspector/blackboard descriptors, builtin Dify/shader/ERD/knowledge fixtures, and public/template conformance coverage. egui now renders descriptor-driven controls and repeatables, writes edits through store transactions, invalidates measured internals on direct edits, exposes dropped-wire/inspector authoring descriptors, and keeps dynamic repeatable port handles tied to item regions. GPUI proof now consumes builtin semantic descriptors through local component-layout projection while honestly reporting `ProjectionFallback` for real layout-pass bounds.
* **Authoring-contract review fixes**: Fixed review findings for conformance overclaim gaps, unresolved repeatable action refs, item-specific action ids, numeric repeatable item ids, dynamic repeatable port anchors in egui measurement/handle layout, direct egui control/repeatable dirty invalidation, and the GPUI resize proof that previously did not actually resize the node. The disabled-action conformance surface remains descriptor/applicability proof only; runtime still does not own toolkit action execution.
* **Authoring-contract final verification**: Re-ran `cargo fmt --all -- --check`, `cargo fmt --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml --check`, `git diff --check`, `git -C repo-ref/open-gpui diff --check`, `cargo nextest run -p jellyflow-runtime -p jellyflow-egui -p jellyflow-proof --lib --no-fail-fast` (529 tests), `cargo test -p jellyflow-runtime --test public_surface -- --nocapture`, `cargo test -p jellyflow-proof --test proof -- --nocapture` (3 tests), `cargo test --manifest-path templates/headless-adapter/Cargo.toml` (22 tests), `cargo check -p jellyflow-egui --examples`, `cargo run -p jellyflow-egui --example gallery_snapshot -- target/jellyflow-egui-gallery`, and GPUI canvas-jellyflow check/test (10 tests); all passed.
* **Authoring-contract commits**: Committed the main-repo authoring-contract implementation as `0049be6 feat(node-ui): add authoring contracts` and the open-gpui GPUI authoring surface proof as `5ee925a feat(canvas): project jellyflow authoring surfaces`; `repo-ref/open-gpui` local `main` is ahead of origin by that proof commit.
* **Open-GPUI mature adapter planning**: Created `docs/plans/2026-06-30-002-feat-open-gpui-mature-adapter-plan.md` as the next implementation-ready fearless-refactor plan. The plan narrows maturity work to open-gpui, adds a `jellyflow-open-gpui` adapter boundary, requires a real open-gpui semantic element-bounds hook, moves reusable GPUI logic out of the example, and covers descriptor-driven controls, repeatables, dynamic ports, menus, dropped-wire actions, inspector, and product-fixture regression gates.
* **Open-GPUI mature adapter U8**: Added adapter-owned product fixture regression gates in `jellyflow-open-gpui::testing` for Dify-style LLM, shader/blueprint dynamic inputs, ERD repeatable fields, mind-map shell content, dropped-wire menus, and inspector state. The `canvas-jellyflow` example now consumes those gates instead of owning the reusable regression logic; GPUI capability docs still require honest `ProjectionFallback` reporting unless real open-gpui element bounds back a full layout-pass claim.
* **Open-GPUI mature adapter U9 cleanup**: Removed current-facing "GPUI proof" language from the open-gpui Jellyflow fixture and refreshed README/Node UI Kit docs so the final story is `jellyflow-open-gpui` as the adapter boundary, `canvas-jellyflow` as a consumer/manual smoke fixture, and `ProjectionFallback` as the honest capability state until layout-pass element bounds drive measurement.
* **Open-GPUI mature adapter U9 verification**: Re-ran main/open-gpui format and diff checks, `cargo nextest run -p jellyflow-open-gpui --no-fail-fast` (25 tests), GPUI canvas-jellyflow check, and GPUI canvas-jellyflow tests (13 tests); all passed.
* **Open-GPUI mature adapter final verification**: Completed the plan's final verification contract: main/open-gpui format and diff checks, `jellyflow-open-gpui` nextest (25 tests), runtime/egui/proof lib nextest (529 tests), runtime public-surface tests, proof integration tests, headless adapter template tests, egui examples check, GPUI canvas-jellyflow check/test, open-gpui measured-element nested-bounds test, and a short GPUI launch smoke all passed. Existing open-gpui `gpui_macos` check-cfg/unused-unsafe warnings remain out of scope.
* **Open-GPUI mature adapter review closeout**: Final local review confirmed `jellyflow-open-gpui` is the only Jellyflow adapter boundary that depends on the local Open GPUI fork, runtime/core/layout remain toolkit-free, `canvas-jellyflow` consumes reusable adapter helpers, and `ProjectionFallback` cannot claim full layout-pass support. A read-only auxiliary review agent was started but interrupted after repeated timeouts; no unresolved blocking finding was returned before interruption.
* **Open-GPUI layout-pass measurement planning**: Created `docs/plans/2026-06-30-003-feat-open-gpui-layout-pass-measurement-plan.md` as the next implementation-ready plan. The plan narrows the next fearless-refactor stage to wiring open-gpui `measured_element` reports into live `canvas-jellyflow` node internals, publishing real layout-pass bounds through `jellyflow-open-gpui` into `NodeMeasurement`, and gating handles, edges, hover, inspector, dynamic repeatables, and capability reports on measured-element source coverage while preserving explicit `ProjectionFallback`.
* **Open-GPUI layout-pass measurement implementation**: Implemented the layout-pass measurement plan. `jellyflow-open-gpui` now exposes stable `OpenGpuiMeasurementId`, layout-pass measured regions, source coverage, coverage-gated `OpenGpuiAdapter::layout_pass(coverage)`, inspector target bounds, and product-shaped layout-pass fixture gates for Dify workflow, shader/blueprint, ERD, and mind-map shapes.
* **Open-GPUI live measured consumer**: Updated `repo-ref/open-gpui/examples/canvas-jellyflow` to wrap visible slots, scoped controls, repeatable item rows, and anchors with `measured_element`; consume collected bounds into `NodeGraphStore`; route handles, hit testing, invalid connection feedback, and inspector target highlights through fresh measurement facts; and downgrade dirty measurements to projection fallback until the next layout pass.
* **Open-GPUI review fix**: Local review found that identical layout-pass facts would still bump `measurement_revision`, causing `NodeGraphStore::report_node_measurement` to publish changed facts every frame. The example now reuses the previous revision when fresh geometry is unchanged, and `unchanged_layout_pass_measurements_reuse_revision` locks the no-refresh path while dirty measurements still bump revision.
* **Open-GPUI layout-pass final verification**: Completed the final verification contract: main/open-gpui format and diff checks, `jellyflow-open-gpui` nextest (32 tests), runtime/egui/proof lib nextest (529 tests), runtime public-surface tests, proof integration tests, headless adapter template tests (22 tests), egui examples check, open-gpui measured-element nested-bounds test, GPUI canvas-jellyflow check/test (17 tests), and a short GPUI launch smoke that reached the running binary before manual interrupt. Existing open-gpui macOS `check-cfg` / `unused_unsafe` warnings remain out of scope.

## 2026-07-01

* **Open-GPUI productized authoring planning**: Ran read-only subagent analysis for GPUI authoring gaps, Open GPUI component APIs, visual/interaction regression, runtime headless boundaries, and `egui-snarl` comparison. The durable conclusion is that the next stage should productize Open GPUI live authoring UX, not add another shared descriptor layer or a shared widget crate.
* **Open-GPUI productized authoring plan**: Created `docs/plans/2026-07-01-001-feat-open-gpui-productized-authoring-plan.md` as the implementation-ready plan for adapter-local renderer registration, live control edits, event arbitration, action/menu and dropped-wire dispatch, repeatable dynamic authoring, inspector/blackboard panels, structured regression gates, and capability/status cleanup.
* **Open-GPUI productized authoring implementation**: Implemented the plan through structured gates: adapter-local authoring controller, live GPUI control edit plans, custom node renderer facade, action/menu and dropped-wire dispatch, repeatable add/remove/reorder/edit with dynamic-port lifecycle evidence, interactive inspector and minimal blackboard panels, and product-shaped regression reports.
* **Open-GPUI regression hardening**: Expanded `jellyflow-open-gpui::testing` with density, resize, region-source, blackboard, dropped-wire, inspector target, invalid hover, editable-control, and repeatable mutation evidence. The `canvas-jellyflow` example consumes these gates and currently passes 22 focused tests.
* **Open-GPUI capability cleanup**: Updated capability/status wording to reflect the current state: live `measured_element` layout-pass consumption exists, but full layout-pass capability remains coverage-gated; projection/partial coverage means initial, dirty, hidden, missing, duplicate, or partial measurement rather than a missing Open GPUI bounds hook.
* **Open-GPUI productized authoring review hardening**: Fixed late review findings after the initial implementation. `jellyflow-open-gpui` now uses the store's real interaction state for dropped-wire insert planning, exposes repeatable add/remove disabled reasons, and expects repeatable item measurement ids to use item slot keys. Runtime exposes shader properties as a repeatable collection so blackboard actions can share the repeatable authoring path. `canvas-jellyflow` now replans control edits from the live store, executes blackboard/repeatable semantic actions instead of logging no-ops, scopes node-internal element ids by node/surface, and renders unavailable controls with disabled/read-only component states.
* **Open-GPUI productized authoring final verification**: Completed the final verification contract: main/open-gpui format and diff checks, `jellyflow-open-gpui` nextest (54 tests), runtime/egui/proof lib nextest (529 tests), runtime public-surface tests, proof integration tests, headless adapter template tests (22 tests), egui examples check, open-gpui measured-element nested-bounds test, GPUI canvas-jellyflow check/test (25 tests), and a short GPUI launch smoke that reached the running binary before manual interrupt. Existing open-gpui macOS `check-cfg` / `unused_unsafe` warnings remain out of scope.
* **Open-GPUI authoring facade cleanup plan**: Created and began executing `docs/plans/2026-07-01-002-refactor-open-gpui-authoring-facade-plan.md` to move reusable authoring glue out of `canvas-jellyflow` while keeping concrete widgets and host lifecycle local to Open GPUI.
* **Authoring facade U1**: Centralized adapter-local JSON binding in `jellyflow-open-gpui`, replacing duplicated control/repeatable/inspector helper logic for dot paths, JSON pointers, `Slot` lookup, field-row fallback, scalar ids, and repeatable item lookup. Committed as `8e8768e refactor(open-gpui): centralize adapter json binding`; adapter nextest passed with 58 tests.
* **Authoring facade U2**: Added live-store control planning to `OpenGpuiAuthoringController` so widget callbacks read current `NodeGraphStore` data at dispatch time rather than captured node snapshots. The `canvas-jellyflow` example now calls the adapter helper. Committed as `7394455 feat(open-gpui): plan controls from live store` plus `repo-ref/open-gpui` commit `fcdb60a`.
* **Authoring facade U3**: Moved generic semantic repeatable action mapping and store application into `jellyflow-open-gpui`, preserving host-supplied add-item factories for demo defaults and explicit skipped outcomes for unsupported intents. Committed as `c2ed930 feat(open-gpui): map repeatable action dispatch` plus `repo-ref/open-gpui` commit `352fa2e`.
* **Authoring facade U4**: Added `element_ids` scoped helper APIs and `OpenGpuiNodeRendererRegistry::render_with_host`, giving host renderers semantic context, measurement/id helpers, fallback reasons, and generic host services without exposing Open GPUI element types upstream. `canvas-jellyflow` now consumes the facade and adapter id helpers instead of duplicating renderer routing/id construction. Committed as `e201f37 feat(open-gpui): add renderer host facade` plus `repo-ref/open-gpui` commit `96f6b4f`.
* **Authoring facade U4 verification**: Re-ran `cargo fmt --all -- --check`, `cargo fmt --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml -- --check`, main/open-gpui diff checks, `cargo nextest run -p jellyflow-open-gpui --no-fail-fast` (68 tests), GPUI canvas-jellyflow check, and GPUI canvas-jellyflow tests (25 tests). Existing open-gpui macOS `check-cfg` / `unused_unsafe` warnings remain out of scope.
* **Authoring facade U5**: Refreshed `jellyflow-open-gpui` README, node authoring regression docs, and engineering memory to document the refined ownership split: adapter-owned binding/live planning/repeatable mapping/ids/renderer facade, host-owned concrete Open GPUI widgets, focus/popup state, weak-entity dispatch, demo defaults, visual layout, and refresh notifications. Committed as `179e4fb docs(open-gpui): document authoring facade ownership`.
* **Authoring facade review fix**: Local final review found a real scoped-id edge case: raw `:`-separated element id segments could collide for external semantic keys containing `:`, and the custom renderer badge id was not node scoped. `jellyflow-open-gpui` now escapes dynamic id segments and `canvas-jellyflow` passes the node id for custom renderer badges. Committed as `daaf27b fix(open-gpui): harden adapter element ids` plus `repo-ref/open-gpui` commit `653158d`.
* **Authoring facade focused refresh**: Re-ran `cargo nextest run -p jellyflow-open-gpui --no-fail-fast` (70 tests) and `cargo test --manifest-path repo-ref/open-gpui/examples/canvas-jellyflow/Cargo.toml --bin open-gpui-canvas-jellyflow -- --nocapture` (25 tests); both passed. Existing open-gpui macOS `check-cfg` / `unused_unsafe` warnings remain out of scope.
* **Authoring facade final verification**: Completed the final verification contract after the scoped-id fix: main/open-gpui format and diff checks, `jellyflow-open-gpui` nextest (70 tests), runtime/egui/proof lib nextest (529 tests), runtime public-surface tests, proof integration tests, headless adapter template tests (22 tests), egui examples check, open-gpui measured-element nested-bounds test, GPUI canvas-jellyflow check/test (25 tests), and a short GPUI launch smoke all passed. The launch smoke reached the running binary and was manually interrupted; existing open-gpui macOS `check-cfg` / `unused_unsafe` warnings remain out of scope.
* **Open-GPUI node component kit planning**: Created `docs/plans/2026-07-01-003-feat-open-gpui-node-component-kit-plan.md` as the next implementation-ready plan. The plan narrows the next stage to a host-local Open GPUI node component kit, product renderer presets, Dify/shader/ERD/mind-map native gallery fixtures, structured host-level visual/interaction reports, dynamic repeatable/port honesty, optional screenshot smoke, and docs/memory updates while keeping runtime headless and avoiding a shared widget crate.
* **Open-GPUI component kit U1-U3**: Added widget-free product fixture catalog/report contracts, renderer registry preset ergonomics, and fallback metadata in `jellyflow-open-gpui`. Committed as `d666db6 feat(open-gpui): add product host report contract`, `1e2f240 feat(open-gpui): add renderer registry presets`, and matching Open GPUI local commits through `262f764 feat(canvas): add jellyflow product renderers`.
* **Open-GPUI component kit U4-U6**: `canvas-jellyflow` now has a host-local `node_component_kit`, product renderers for Dify workflow, shader/material, ERD, and mind-map/source cards, a switchable product gallery, structured visual interaction gates, and explicit dynamic repeatable port lifecycle diagnostics. Main repo commits include `445e344 test(open-gpui): add host visual interaction gate` and `9eb209c test(open-gpui): gate dynamic repeatable lifecycle`; Open GPUI local commits include `f1ef932 feat(canvas): add jellyflow product gallery`, `5df03de test(canvas): add jellyflow visual regression gate`, and `7667eb9 feat(canvas): surface repeatable port diagnostics`.
* **Open-GPUI screenshot smoke U7**: Added a test-only `canvas-jellyflow` screenshot exporter that writes nonblank PNG review artifacts under `repo-ref/open-gpui/target/open-gpui-jellyflow-gallery/` when a headless renderer is available. Verified four product fixture PNGs (`workflow-review`, `shader-material-mix`, `erd-customer-orders`, `mind-map-strategy`) and committed the Open GPUI slice as `8225bbd test(canvas): add jellyflow gallery screenshot smoke`.
* **Open-GPUI component kit final verification**: Completed the final verification contract for `docs/plans/2026-07-01-003-feat-open-gpui-node-component-kit-plan.md`: main/open-gpui format and diff checks, `jellyflow-open-gpui` nextest (73 tests), runtime/egui/proof lib nextest (529 tests), runtime public-surface tests, proof integration tests, headless adapter template tests (22 tests), egui examples check, Open GPUI measured-element nested-bounds test, GPUI canvas-jellyflow check/test (28 tests), screenshot smoke artifacts for all four product families, and a short GPUI launch smoke that reached a running binary before external termination. Existing open-gpui macOS warning noise remains out of scope.
